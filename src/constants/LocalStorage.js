const LocalStorage = Object.freeze({
  AppSettings: {
    key: "appSettings",
    settings: "settings",
  },

  Auth: {
    key: "auth",
    token: "token",
    refreshToken: "refreshToken",
  },
});

export default LocalStorage;
